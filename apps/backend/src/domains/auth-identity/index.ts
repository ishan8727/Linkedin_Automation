/**
 * Domain 1: Auth & Identity
 * 
 * Owns: User
 * 
 * Responsibilities:
 * - User management
 * - Does NOT manage sessions internally (auth provider managed)
 * 
 * Phase 3: Service interface implemented
 */

export { AuthIdentityService } from './service';

